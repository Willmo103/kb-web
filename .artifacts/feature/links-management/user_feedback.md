# User Feedback - Links Management and CLI Timeout Fix

The user reported the following feedback:
- The branch has been merged into production.
- The CLI is timing out when trying to import URLs to the server. The server seems to get the import, but the AI process is somehow broken.
- Need to support creating and saving regular links to webpages (URL + description) without ingesting them.
- Track link usage/clicks in the background.
- Support importing links from chromium standard bookmark HTML export files.
