# User Feedback

The user requested:
1. Fix the YouTube video offline badge checking (resolving why the cloud stream icon displays despite video availability).
2. Save downloaded YouTube video files with descriptive names that tie them to the record, but are usable/identifiable without the database (e.g. including uploader/creator and title in the filename, e.g. `[Creator] - Title [VideoId].mp4` format).
3. Implement this on a new branch `feature/descriptive-video-names`.
